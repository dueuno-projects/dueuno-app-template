# i18n

## Specific Cases

- Use `dueuno.elements.style.TextDefault` values whenever possible instead of defining custom text.
- For checkbox labels, the i18n key in `messages.properties` must always end with the `.text` suffix.
